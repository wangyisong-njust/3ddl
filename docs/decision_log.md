# Decision Log

| Date | Decision | Reason | Status | Evidence |
| --- | --- | --- | --- | --- |
| 2026-03-29 | Do not call TRT FP16 “lossless” yet | SN009 shows speedup, but class-3/class-4 segmentation and metrology outputs drift materially | Active | `optimization_history/080_trt_accuracy_risk.md` |
| 2026-03-29 | Do not rely on voxel accuracy alone | Background dominates the volume, so high voxel accuracy can hide defect-class regressions | Active | `optimization_history/080_trt_accuracy_risk.md` |
| 2026-03-29 | Treat I/O as a first-class optimization target | Current default `intelliscan` path still writes and re-reads many intermediate files | Active | `optimization_history/030_pipeline_io_optimization.md` |
| 2026-03-29 | Adaptive margin is the best next engineering bet | Batch and direct-inference paths already exist in code, but SN009 shows `0` batchable crops because expanded boxes exceed ROI | Active | `optimization_history/040_batch_inference.md`, `optimization_history/050_sliding_window_bypass.md` |
| 2026-03-29 | Real calibration data is required before trusting INT8 | Current INT8 builder uses random calibration tensors, which is not enough for defect-sensitive deployment claims | Active | `optimization_history/013_onnx_export_and_engine_build.md` |
| 2026-03-29 | Rebenchmark AMP/BF16 before claiming acceleration | Existing patch benchmark artifact shows AMP inference slower than FP32 for the recorded environment | Active | `optimization_history/014_single_patch_benchmarks.md` |
| 2026-03-29 | Every new optimization must be proven by comparison experiments | Same-sample baseline vs variant runs are needed to separate real gains from noise or output drift | Active | `metrics_definition.md`, `templates/experiment_template.md` |
| 2026-03-29 | Keep adaptive margin experimental, not default | SN009 improved from `0` to `113` direct crops and cut segmentation time by `39.20%`, but segmentation and metrology outputs drifted | Active | `optimization_history/095_adaptive_margin_activation.md` |
| 2026-03-29 | Treat adaptive-margin drift as primarily context-driven | On SN009, context-only (`A vs C`) already reproduces the full adaptive drift, while `C vs D` is nearly identical | Active | `optimization_history/096_adaptive_margin_factor_ablation.md` |
| 2026-03-29 | Do not treat `force-direct-full-crop` as a deployment candidate | Same-crop direct-full inference drifts heavily on both SN009 and SN002 and is useful only as an ablation control | Active | `optimization_history/096_adaptive_margin_factor_ablation.md` |
| 2026-03-29 | Direct-padded vs sliding-window is already close enough on tested fit-crop cases | SN002 same-crop path-only comparison shows near-identity and zero defect flips, so the next effort should target context guardrails instead | Active | `optimization_history/096_adaptive_margin_factor_ablation.md` |
| 2026-03-29 | Treat guarded adaptive margin as a constrained policy-search problem | The safe region is narrow: strict guardrails preserve quality but unlock almost no fast path, while looser thresholds recover speed at measurable quality cost | Active | `optimization_history/097_guarded_adaptive_margin_search.md` |
| 2026-03-29 | Keep `G5` (`xy<=12`, `z<=24`) only as an experimental option | On SN009 it is the best speed-vs-quality tradeoff in this pass, but it still causes one pad-defect flip and has only been checked on two samples | Active | `optimization_history/097_guarded_adaptive_margin_search.md` |
| 2026-03-29 | Business acceptability for guardrails is defined by class-3/class-4 stability plus metrology, not by voxel agreement alone | `G5` keeps much higher voxel agreement than full adaptive, but the real reason it is better is the large drop in defect flips and BLT error | Active | `optimization_history/097_guarded_adaptive_margin_search.md` |
| 2026-03-29 | The current guarded `z` boundary behaves like a cliff on `SN009` | `z=21/22/23` are all near-no-op, while `z=24` is the first threshold that materially opens the fast path | Active | `optimization_history/098_guarded_margin_z_boundary.md` |
| 2026-03-29 | Do not spend more local tuning effort below `z=24` with the current crop-selection logic | The tested `z=21..23` region did not reveal a better intermediate tradeoff than `G5` | Active | `optimization_history/098_guarded_margin_z_boundary.md` |
| 2026-03-29 | Keep combined segmentation + metrology as the default `intelliscan` path | On validated `SN002` and `SN009` runs, segmentation output is identical and corrected metrology CSV matches the combined path while the target stage gets faster | Active | `optimization_history/031_combined_seg_metrology_default.md` |
| 2026-03-29 | Treat the legacy separate metrology `bb` column as previously unreliable | The old code paired bbox metadata by loop index after lexicographic filename sorting, so `pred_10` could receive the wrong bbox record | Active | `optimization_history/031_combined_seg_metrology_default.md` |
| 2026-03-29 | Combined default started as a safe partial I/O optimization before later follow-up cleanup | At the time of `031`, report and GLTF still depended on per-bbox artifacts; later `032` and `033` removed those combined-path dependencies | Active | `optimization_history/031_combined_seg_metrology_default.md`, `optimization_history/032_report_without_bbox_nifti.md`, `optimization_history/033_gltf_without_bbox_pred_nifti.md` |
| 2026-03-29 | Keep true in-memory detection experimental, not default | The first checked-in no-write path was exact on `SN009` and much faster on the front-end, but `SN002` still showed small non-zero bbox / segmentation / metrology drift | Superseded | `optimization_history/056_true_inmemory_detection.md`, `optimization_history/057_detection_parity_quantization.md` |
| 2026-03-29 | Do not assume reproducing the JPEG transform alone guarantees detector equivalence | Matching the JPEG transform narrowed the gap, but did not explain the remaining `SN002` drift by itself | Superseded | `optimization_history/056_true_inmemory_detection.md`, `optimization_history/057_detection_parity_quantization.md` |
| 2026-03-29 | Treat the first disk-backed report refactor as an intermediate failed optimization attempt, not the kept design | Removing `mmt/img` alone is not enough; reloading large NIfTI volumes during report generation gave back the saved I/O time | Active | `optimization_history/032_report_without_bbox_nifti.md` |
| 2026-03-29 | Report reconstruction from the input NIfTI and full segmentation is a safe post-inference change | Validated `bb3d`, `segmentation`, and sorted metrology outputs stayed unchanged on `SN002` and `SN009` | Active | `optimization_history/032_report_without_bbox_nifti.md` |
| 2026-03-29 | Keep the report refactor in its memory-reuse form | Reusing already-loaded input and segmentation arrays removes `mmt/img` without changing outputs, and the final checked-in version is faster on validated `SN002` and `SN009` runs | Active | `optimization_history/032_report_without_bbox_nifti.md` |
| 2026-04-07 | Keep the combined-path no-`mmt/pred` change | The default combined path keeps validated outputs unchanged on `SN002` and `SN009`, removes one more artifact class, and isolated stage benchmarks show the target stage is faster | Active | `optimization_history/033_gltf_without_bbox_pred_nifti.md` |
| 2026-04-07 | Use isolated stage benchmarks, not only single end-to-end runs, when judging small I/O optimizations | Full-pipeline wall-clock varies with JPG conversion and detection, which can hide or invert a real gain inside the target stage | Active | `optimization_history/033_gltf_without_bbox_pred_nifti.md` |
| 2026-04-07 | Do not treat larger direct ROI as a safe geometry-preserving optimization under the current model | Keeping the same crop is not enough; changing only the padded direct-path canvas already causes major segmentation and metrology drift on validated samples | Active | `optimization_history/034_larger_direct_roi_experiment.md` |
| 2026-04-07 | Treat padded-canvas size as a parity-sensitive variable | `SN010 roi118` keeps the same direct/sliding counts as baseline, yet still drifts heavily, so the zero-padded direct shell itself changes the network behavior | Active | `optimization_history/034_larger_direct_roi_experiment.md` |
| 2026-04-07 | Stop prioritizing larger direct ROI under the current direct-padded PyTorch path | Repeated stage-only benchmarking on `SN010` shows `roi124` slower than baseline even where direct-path count increases, while `SN002` and `SN010` show severe defect-sensitive drift | Active | `optimization_history/034_larger_direct_roi_experiment.md` |
| 2026-04-07 | Treat legacy detection `.txt` precision as a parity-sensitive part of the pipeline | Pre-quantization `--ramdisk-detection` on `SN002` reproduced the same small drift as the earlier no-write path; after restoring the legacy two-decimal quantization before bbox merge, both `SN002` and `SN009` became exact | Active | `optimization_history/057_detection_parity_quantization.md` |
| 2026-04-07 | Keep quantized `--inmemory` as the preferred no-write detection path | On validated `SN002` and `SN009` runs it is exact and materially faster than baseline, while `--ramdisk-detection` is only needed as a factor-isolation harness | Active | `optimization_history/057_detection_parity_quantization.md` |
| 2026-04-07 | Switch default detection to quantized in-memory, but keep a file-based fallback | The quantized no-write path is exact on all `8 / 8` validated local raw scans and improves total time by `17.79%` to `29.31%` across the set | Active | `optimization_history/058_quantized_inmemory_multisample_validation.md` |
| 2026-04-07 | Parallelize only the CPU-side detection image preparation, not the detector or merge logic | The remaining front-end bottleneck after `058` is slice-to-image preparation; this can be accelerated without changing bbox quantization, YOLO routing, or downstream geometry | Active | `optimization_history/059_detection_image_worker_parallelism.md` |
| 2026-04-07 | Use `detection_image_workers=4` as the new default | On the local eight-sample raw-scan set, `workers=4` keeps `bb3d.npy`, `segmentation.nii.gz`, and `metrology.csv` identical while improving total time by `10.70%` to `23.09%` relative to the previous quantized in-memory default; `workers=8` was worse than `4` on the controlled SN009 search | Active | `optimization_history/059_detection_image_worker_parallelism.md` |
| 2026-04-07 | Treat detection image preparation as a real throughput target in its own right | After detection parity was stabilized, a pure CPU-side prep change still yielded about `36%` detection-stage speedup on average, which shows front-end preprocessing can remain a first-order runtime bottleneck | Active | `optimization_history/059_detection_image_worker_parallelism.md` |
| 2026-04-07 | Overlap next-batch CPU preparation with current YOLO inference inside the kept in-memory detection path | After `workers=4`, the next remaining serial bottleneck was batch-level preparation. This can be overlapped without changing detector numerics, bbox quantization, or downstream geometry | Active | `optimization_history/061_detection_batch_prefetch_overlap.md` |
| 2026-04-07 | Switch `detection_batch_prefetch` on by default | On the local eight-sample raw-scan set, batch prefetch keeps `bb3d.npy`, `segmentation.nii.gz`, and `metrology.csv` identical while improving total time by `14.20%` and detection time by `34.64%` relative to the previous `workers=4` default | Active | `optimization_history/061_detection_batch_prefetch_overlap.md` |
| 2026-04-07 | Do not keep same-GPU cross-view detection overlap under the current single-YOLO path | Repeated detection-only benchmarks show the temporary view-prefetch path is slower on both `SN009` and `SN002`, even though outputs remain exact | Active | `optimization_history/062_detection_view_prefetch_negative_result.md` |
| 2026-04-07 | Treat higher-level overlap as a resource-allocation problem, not as “more concurrency is always better” | The kept intra-view batch overlap helps because it hides CPU prep behind existing GPU work; the rejected cross-view overlap adds CPU contention without creating new GPU parallelism | Active | `optimization_history/061_detection_batch_prefetch_overlap.md`, `optimization_history/062_detection_view_prefetch_negative_result.md` |
| 2026-04-07 | Keep multi-GPU batch dispatch as the next safe throughput layer | Independent files can be scheduled across multiple GPUs with unchanged per-sample outputs; on the validated four-sample batch, `2` GPUs reduced batch wall clock by `44.65%` versus the `1`-GPU baseline | Active | `optimization_history/063_multigpu_batch_throughput.md` |
| 2026-04-07 | Distinguish throughput optimizations from single-sample latency optimizations in reports and benchmarks | Multi-GPU orchestration materially improves batch wall clock, but it does not change per-file pipeline latency; these claims should not be mixed | Active | `optimization_history/063_multigpu_batch_throughput.md` |
| 2026-04-07 | Frame future writing around defect-aware co-optimization, not isolated speedups | The current evidence is strongest when model compression, pipeline engineering, and runtime choices are evaluated together against defect-sensitive outputs | Active | `publication_strategy.md` |
| 2026-04-07 | Treat negative ablations as part of the contribution, not as failed side notes | The adaptive-margin and TRT studies already show that unsafe speedups can reveal causal insights and justify stricter acceptance criteria | Active | `optimization_history/096_adaptive_margin_factor_ablation.md`, `optimization_history/080_trt_accuracy_risk.md`, `publication_strategy.md` |
| 2026-04-09 | Keep `torch.compile(reduce-overhead)` only as a PyTorch fallback backend, not as the main deployment story | On retained `r=0.5`, compile cuts eager latency from `6.94 ms` to `4.80 ms`, but TRT FP16 is still much faster at `1.62 ms`; on `r=0.375`, compile gains are small | Active | `../wp5-seg/reports/paper_track/020_runtime_backend_calibration_compile_energy.md` |
| 2026-04-09 | Keep TRT FP16 as the preferred retained deployment backend for the current students | On both retained students, FP16 matches PyTorch full-case Dice while delivering the best clean latency-power tradeoff | Active | `../wp5-seg/reports/paper_track/020_runtime_backend_calibration_compile_energy.md` |
| 2026-04-09 | Treat INT8 calibration policy as data-selection-sensitive, not as a plug-in switch | For `r=0.375`, generic real calibration drops average Dice to `0.888706`, while fit-only calibration recovers to `0.892145`; calibration sample selection matters | Active | `../wp5-seg/reports/paper_track/020_runtime_backend_calibration_compile_energy.md` |
| 2026-04-09 | Do not use GPU-direct TRT evaluation under MONAI sliding-window for backend quality claims | The first GPU-direct TensorRT evaluation collapsed to average Dice `0.291838`; only the host-relay path produced trustworthy parity-level results | Active | `../wp5-seg/reports/paper_track/020_runtime_backend_calibration_compile_energy.md` |
| 2026-04-09 | Do not treat `torch.compile()` as a strong current single-sample `intelliscan` backend | Although patch-level compile is useful on `r=0.5`, the whole-pipeline single-scan run becomes much slower because every CLI invocation pays the compile warmup cost | Active | `../wp5-seg/reports/paper_track/021_intelliscan_runtime_candidate_ranking.md` |
| 2026-04-09 | Treat backend ranking and student ranking as separate problems | In the three-sample whole-pipeline ranking, eager, compile, and TRT variants within the same student family have almost identical teacher-relative drift, so backend choice does not solve model-induced metrology drift | Active | `../wp5-seg/reports/paper_track/021_intelliscan_runtime_candidate_ranking.md` |
